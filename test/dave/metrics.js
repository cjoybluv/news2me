var term = 'Jeb Bush';

var channelMetrics = [];


// channelMetrics.push({});

var termMetric = {};
termMetric.term = term;
termMetric.retweet_total = 12314;

channelMetrics.push(termMetric);

console.log(channelMetrics);
